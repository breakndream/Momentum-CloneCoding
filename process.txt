const loginForm = document.getElementById("login-form");
 == document.querySelector("#login-form");

all capitalized letter const : when we want to store a string

const link = document.querySelector("a");
link.addEventListener("click", handleClick)
don't invoke the function with parantheses, just make a reference
 

 
 function handleClick(event) {
     event.preventDefault();
     console.log(event);
 };

=== 

 link.addEventListener("click", function(event){
     event.preventDefault();
     console.log(event);
 })

console.dir() : object의 directory를 인쇄해줌

백틱 사용하기~~~~~~~~~`````
 greeting.innerText = `Hello ${userName}` ===  greeting.innerText = "Hello " + userName;

css:
 visibility:hidden은 공간은 그대로 두고 보이지만 않는건데 display:none은 잡아둔 공간도 사라진다고 하더라고요!


html :
 form 은 submit the form and **refresh** the page automatically


// setTimeout after how many seconds,
// setInterval every other seconds

"0".padStart(2,"0)
"00"
"hello".padStart(20,"x");
'xxxxxxxxxxxxxxxhello'

-> padStart/ padEnd String 에만 됨.

string으로 만드는 방법
1. const hours = date.getHours().toString().padStart(2,"0");
2. const hours = String(date.getHours()).padStart(2,"0");